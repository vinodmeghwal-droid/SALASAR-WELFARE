# Local MongoDB for development: `docker compose up -d`
# Then set backend MONGODB_URI=mongodb://127.0.0.1:27017/hr-welfare
services:
  mongo:
    image: mongo:8
    restart: unless-stopped
    ports:
      - '27017:27017'
    volumes:
      - mongo-data:/data/db

volumes:
  mongo-data:
